name: My Workflow

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          # Replace with your repository URL
          repository: "git@github.com:saikumar21698/github-action.git/test"
          ref: "main"  # Adjust if needed