name: My Workflow

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout tool repo
        uses: actions/checkout@v3
        with:
          # Replace with your repository URL
          repository: "github-action/test"
          path: test
          ref: "main"  # Adjust if needed